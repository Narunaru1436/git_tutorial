ローカルはワークツリー→ステージ→リポジトリの３つのエリアに分かれている

なぜステージがあるのか
→変更したファイルと変更がないファイルのうち、変更があるファイルのみ記録したい場合がある。このとき、変更があるファイルだけをコミットするエリアがステージ

git initでローカルにGit用のフォルダが生成され(隠しフォルダ)、その中にGitに必要なすべてのファイルが入っている

ブランチというのは最新のコミットを指し示したポインタ(master)
ブランチの利点は同じプロジェクトに対して独立して作業ができること

